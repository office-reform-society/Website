{
  "name": "@reform-society/website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && npm run postbuild",
    "postbuild": "mkdir -p dist/terms dist/privacy && cp dist/terms.html dist/terms/index.html && cp dist/privacy.html dist/privacy/index.html",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.14"
  },
  "dependencies": {
    "gsap": "^3.12.0"
  }
}
